 => => naming to docker.io/library/mariadbtest:latest                                                               0.0s
 => => unpacking to docker.io/library/mariadbtest:latest                                                            1.2s
lab1r1s12% docker run -d --name mariadb-test \
  --network inception-net \
  -e MYSQL_DATABASE=wordpress \
  -e MYSQL_USER=wpuser \
  -e MYSQL_PASSWORD=wppassword123 \
  -e MYSQL_ROOT_PASSWORD=rootpassword123 \
  mariadb-test
Unable to find image 'mariadb-test:latest' locally
docker: Error response from daemon: pull access denied for mariadb-test, repository does not exist or may require 'docker login'

Run 'docker run --help' for more information
lab1r1s12% docker run -d --name mariadb-test \
  --network inception-net \
  -e MYSQL_DATABASE=wordpress \
  -e MYSQL_USER=wpuser \
  -e MYSQL_PASSWORD=wppassword123 \
  -e MYSQL_ROOT_PASSWORD=rootpassword123 \
  mariadb-test
Unable to find image 'mariadb-test:latest' locally
docker: Error response from daemon: pull access denied for mariadb-test, repository does not exist or may require 'docker login'

Run 'docker run --help' for more information
lab1r1s12% 
